# Gemini Project Execution

## Execution Order

1.  `splicing_functional_impat.py`
2.  `protein_primary_sequence_impact.py`
3.  `add_frame_shify_density.py`

## Project Rules

- Do not commit data files to the repository.
- Do not commit results files to the repository.
- All code should be in the `src` directory.
- All data should be in the `data` directory.
- All results should be in the `results` directory.